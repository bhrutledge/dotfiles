# ~/.tmuxinator/default.yml

name: es-mac
root: ~/Code/es

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: vagrant up

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
pre_window: workon es && clear

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

windows:
    - editor: $EDITOR
    - shell:
        layout: f5ef,272x82,0,0[272x59,0,0,23,272x22,0,60{135x22,0,60,24,136x22,136,60,25}]
        panes:
            - cd es-site && clear
            - browser-sync:
                - cd es-site/es/es
                - browser-sync start --no-open --port 3000 --ui-port 3001
                  --proxy http://es.local:8000
                  --files "static/css/**/*.css, static/js/**/*.js, templates/**/*.html"
            - compass:
                - cd es-site/es/es/static
                - compass watch
